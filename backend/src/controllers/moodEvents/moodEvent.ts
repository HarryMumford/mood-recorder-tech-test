// import express, { Request, Response } from "express";

// export const moodEventController = express.Router();

// moodEventController.get('/', async (req: Request, res: Response) => {
//     try {
//         const moodEvents:  = await MoodEventService.findAll();

//         res.status(200).send(items);
//     } catch (e) {
//         res.status(404).send(e.message);
//     }
// });

